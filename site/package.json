{
  "name": "judgement-odds-site",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "predev": "node scripts/parse-combatants.mjs && node scripts/parse-artefacts.mjs",
    "dev": "vite",
    "prebuild": "node scripts/parse-combatants.mjs && node scripts/parse-artefacts.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "@mdi/font": "^7.4.47",
    "vue": "^3.5.13",
    "vuetify": "^3.8.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.0.3",
    "vite-plugin-vuetify": "^2.1.0",
    "vitest": "^3.0.5"
  }
}
